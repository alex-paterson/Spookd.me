<footer>
	<div>
		<span><a href="<%=root_url%>">Home</a></span>
		<% if current_user %>
			<span><a href="<%=account_url%>">Account</a></span>
			<span><a href="<%=signout_url%>">Log Out</a></span>
		<% else %>
			<span><a href="/auth/facebook">Sign In</a></span>
		<% end %>
		<span><a href="<%=claim_url%>">Claim A Story</a></span>
	</div>
		
	<div style="text-align: right;">
		<span><a href="<%=root_url%>" style="font-size: 2em;">SPOOKD.ME</a></span>
	</div>
</footer>